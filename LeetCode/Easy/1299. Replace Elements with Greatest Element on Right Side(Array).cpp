#include <bits/stdc++.h>
using namespace std;
class Solution
{
public:
    vector<int> replaceElements(vector<int> &arr)
    {
        //[ 17, 18, 5, 4, 6, 1 ]
        int n = arr.size(), last = -1, temp = 0;
        for(int i = 0; i < n; i++){
            
        }

    }
};