#include <bits/stdc++.h>
#include "717. 1-bit and 2-bit Characters(Arrray).cpp"
using namespace std;

int main()
{
    Solution s;
    int a;
    vector<int> nums = {1, 1, 1, 0};
    a = s.isOneBitCharacter(nums);
    cout << "Ans = " << a << endl;
    return 0;
}