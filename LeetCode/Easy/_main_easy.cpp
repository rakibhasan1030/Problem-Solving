#include <bits/stdc++.h>
#include "1652. Defuse the Bomb (Array).cpp"
using namespace std;
int main()
{
    Solution s;
    vector<int> code = {2, 4, 9, 3};
    int k = -2;
    s.decrypt(code, k);
    return 0;
}