#include<bits/stdc++.h>
using namespace std;
int main(){
    string  s = "?zs", ans;
    char a = 'a';
    int a_ = (int)a;
    //cout << char((int)a + 1);
    for (int i = 0; i < s.size(); i++){
        if(s[i] == '?'){

        }else{

        }
    }

    return 0;
}
