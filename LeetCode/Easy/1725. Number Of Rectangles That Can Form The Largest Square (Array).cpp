#include <bits/stdc++.h>
using namespace std;
class Solution {
public:
    int countGoodRectangles(vector<vector<int>>& rectangles) {
        vector<int> unique; 
        int m; 
        for(auto rec : rectangles){
            int j = 0;
            cout << max(rec[j], rec[j+1]);
            cout<<endl;
        }
    }
};