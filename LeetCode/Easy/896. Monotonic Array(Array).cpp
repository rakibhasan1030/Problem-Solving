#include <bits/stdc++.h>
using namespace std;
class Solution {
public:
    bool isMonotonic(vector<int>& nums) {
        int len = nums.size();
        bool ans = false;
        for(auto num : nums){
            
        }
    }
};