#include <bits/stdc++.h>
using namespace std;
class Solution {
public:
    int findLUSlength(string a, string b) {
        for(int i = 0; i < a.length(); i++){
            for(int i = 0; i < a.length(); i++){
            
        }   
        }
    }
};